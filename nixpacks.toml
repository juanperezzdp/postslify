[variables]
NODE_VERSION = "20.11.1"
HOSTNAME = "0.0.0.0"
PORT = "3000"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = ["npm run build", "cp -R .next/standalone/* ."]

[start]
cmd = "sh -c \"node server.js & node scripts/local-cron.js & wait\""
